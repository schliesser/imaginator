# cat=Processing; type=options[Local async (middleware + 302)=local:async,Local sync (classic rendering on first page load)=local:sync,imgproxy (offloaded)=imgproxy]; label=Image processor
processor = local:async

# cat=External; type=string; label=imgproxy: base URL of the imgproxy endpoint (e.g. https://imgproxy.example)
processorBaseUrl =

# cat=External; type=string; label=imgproxy: HMAC key (hex). Empty = insecure/unsigned URLs (dev only)
processorSignKey =

# cat=External; type=string; label=imgproxy: HMAC salt (hex). Empty = insecure/unsigned URLs (dev only)
processorSalt =

# cat=External; type=string; label=imgproxy: origin prefix prepended to the source path (leave empty when imgproxy has IMGPROXY_BASE_URL set)
processorSourceBaseUrl =

# cat=Processing; type=int+; label=Maximum image dimension (px): the width ladder is capped to this
maxDimension = 2000

# cat=Processing; type=string; label=Width-ladder rungs (comma-separated)
ladder = 320,420,560,740,980,1300,1720,2000

# cat=Processing; type=string; label=Breakpoints for the aspect-ratio element (key:min-width px, comma-separated; xs:0 is the base ratio)
breakpoints = xs:0,sm:576,md:768,lg:992,xl:1200

# cat=Processing; type=options[AVIF=avif,WebP=webp]; label=Output format (single, applied to <img> and every <source>)
format = avif

# cat=Processing; type=string; label=Excluded extensions (comma-separated): served verbatim as a plain <img>, never processed (vector/animated formats)
excludeExtensions = svg,ai,eps,gif

# cat=Quality; type=int+; label=AVIF quality (AVIF's scale sits lower than JPEG/WebP for the same perceived quality)
quality.avif = 50

# cat=Quality; type=int+; label=WebP quality
quality.webp = 72

# cat=Placeholder; type=options[ThumbHash=thumbhash,Dominant color=dominant-color,None=none]; label=Low-quality placeholder
lqip = thumbhash

# cat=Signing; type=string; label=Additional valid signing secrets for key rotation (comma-separated); the active secret is always derived from the global encryptionKey
secretsRotation =
